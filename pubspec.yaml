name: alter
description: ALTER, a voice-first AI Future Operating System.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.10.0 <4.0.0"
  flutter: ">=3.38.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.4
  cupertino_icons: ^1.0.8
  flutter_animate: ^4.5.2
  flutter_lucide: ^1.11.0
  flutter_riverpod: ^3.3.2
  go_router: ^17.3.0
  google_fonts: ^8.1.0
  http: ^1.6.0
  nfc_manager: ^4.2.1
  nfc_manager_ndef: ^1.1.0
  supabase_flutter: ^2.10.3

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
